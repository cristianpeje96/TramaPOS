# --- Framework y servidor ---
fastapi==0.115.0
uvicorn[standard]==0.32.0

# --- Base de datos ---
sqlalchemy==2.0.35
asyncpg==0.30.0
alembic==1.13.3

# --- Validación / configuración ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Autenticación ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- WebSockets (agente de hardware, notificaciones en tiempo real) ---
websockets==13.1

# --- Tareas en segundo plano (facturación DIAN pesada) ---
celery==5.4.0
redis==5.1.1

# --- HTTP client (llamadas al proveedor DIAN) ---
httpx==0.27.2

# --- Carga masiva de productos (plantilla Excel) ---
openpyxl==3.1.5
python-multipart==0.0.12

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0